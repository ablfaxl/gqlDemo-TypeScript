export interface writerData {
    name: string;
    createdAt: string;
    _id: string;
  }

export interface bookData {
    _id: string;
    title: string;
    authorId:string;
    createdAt:string;
}